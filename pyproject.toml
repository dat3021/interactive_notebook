[project]
name = "notebook"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "datpham", email = "phamthanhdat387@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.62.0",
]

[project.scripts]
notebook = "notebook:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
